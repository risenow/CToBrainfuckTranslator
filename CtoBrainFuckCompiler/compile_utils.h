#pragma once
/*
#include <string>
#include "compile_utils_defs.h"

class CMemoryManager;

std::string jump_to(address_t& memory_pos, address_t new_pos);
std::string cell_to_null(CMemoryManager* memory_manager, address_t cell_pos);

address_t get_int_var_sign_cell_address(const CVariableMemoryLocation& v);
address_t get_int_var_value_cell_address(const CVariableMemoryLocation& v);*/